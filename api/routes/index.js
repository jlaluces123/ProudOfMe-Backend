const authRouter = require('./auth');
const usersRouter = require('./users');
const profileRouter = require('./profile');
const momentRouter = require('./moments');

module.exports = {
    authRouter,
    usersRouter,
    profileRouter,
    momentRouter,
};
